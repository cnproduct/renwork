# 品牌设计系统与前端美化规范

## 1. 设计目标

统一 rrenn.com 与 RenWork 的品牌识别，但保持各自的使用场景：

- 官网：可信、清晰、国际化的企业服务品牌，优先表达业务结果与真实证据；
- RenWork：高密度、长时间使用的桌面生产力工具，优先效率、状态和可解释性；
- 两者共享 Logo 使用规则、核心橙色、排版语气、图标语言和数据状态语义；
- 不强迫官网和桌面端像素级相同。

## 2. 品牌资产门禁

### 2.1 人人易企业 Logo

官网 Header/Footer 使用用户现有锁定版 Logo：深色 `rr` 圆角符号、橙色购物车式图形、橙色描边“人人易”和深色 `nn` 字样。Codex 不得重画、拆字、换图标、添加渐变或使用 AI 生成的近似 Logo。

上线前品牌负责人提供：

- `rrenn-logo-horizontal.svg`（主版本）；
- `rrenn-logo-horizontal-reversed.svg`；
- `rrenn-symbol.svg`；
- PNG 1x/2x fallback；
- favicon/app icon；
- 资产版本与审批记录。

现有 530×198 PNG 仅可作为临时审校参考；正式网页优先使用干净 SVG。

### 2.2 RenWork 产品 Logo

现有仓库包含橙/青火箭式产品标记，但不能推断这是用户最终批准版本。P0 必须由用户确认 RenWork 正式 SVG。未确认前：

- 不在官网重新设计产品 Logo；
- RenWork 内部保持现状，避免视觉回归；
- 新页面使用文字 `RenWork` + “产品 Logo 待确认”的资产槽；
- 不用人人易 Logo 直接覆盖应用 icon。

## 3. 视觉方向

### 3.1 官网比例

- 70% 暖白/浅中性色：提高可信与阅读效率；
- 20% 深墨色：标题、页脚和关键对比区；
- 10% 品牌橙：CTA、选中和关键强调。

避免整站黑色赛博风、连续霓虹描边、粒子背景和与外贸无关的抽象 AI 大脑。深色 Hero 可以存在，但后续内容应回到明亮、真实、可读的企业场景。

### 3.2 图像语言

优先：真实工厂、产品应用、展会/采购沟通、物流/港口、产品界面、数据证据局部、团队协作。每张图片必须回答“这与客户开发哪个环节相关”。

禁止：未经授权客户 Logo、假团队照、廉价握手图库、把中国企业刻板化、无法解释的数据可视化、带有第三方平台隐私的截图。

产品截图使用统一 16:10/3:2 容器、12–16px 圆角、1px 边框和柔和阴影；敏感字段脱敏，Demo 水印明确。

## 4. 颜色系统

### 4.1 原始品牌色

从现有 Logo 参考提取：

| Token | 值 | 用途 |
|---|---:|---|
| `brand-ink` | `#211513` | 主标题、深色背景 |
| `brand-orange-500` | `#E06A14` | 图标、装饰、非小号文字强调 |
| `warm-white` | `#FDFCFB` | 页面主背景 |

`#E06A14` 配白色对比约 3.36:1，不适合作为白色普通文字的按钮背景。可访问性交互色：

| Token | 值 | 用途 |
|---|---:|---|
| `brand-orange-600` | `#C55208` | 白字主按钮，最低可用 |
| `brand-orange-700` | `#B54708` | hover/强调 |
| `brand-orange-800` | `#9A3B06` | active/小文本强调 |
| `functional-teal-700` | `#0F766E` | 成功、验证、关系信号 |
| `functional-blue-700` | `#0369A1` | 信息、链接、数据 |

### 4.2 语义色

```css
:root {
  --brand-ink: #211513;
  --brand-orange: #e06a14;
  --brand-orange-action: #c55208;
  --brand-orange-hover: #b54708;
  --surface-page: #fdfcfb;
  --surface-card: #ffffff;
  --surface-subtle: #f7f3ef;
  --text-primary: #211513;
  --text-secondary: #625b58;
  --border-default: #ded7d2;
  --focus-ring: #0369a1;
  --status-info: #0369a1;
  --status-success: #0f766e;
  --status-warning: #a65f00;
  --status-danger: #b42318;
}
```

RenWork 应把这些映射到现有 CSS variables/token，不直接大面积替换 `index.css`。必须同时验证 dark theme，不能只覆盖 light。

### 4.3 使用规则

- 橙色用于行动和品牌，不用于所有卡片边框；
- 成功不使用橙色，使用 teal/green；危险使用 red；
- 同一状态不能在不同页面变色；
- 颜色不是唯一状态表达，配合图标、文字和形状；
- 焦点环使用蓝色以区别橙色品牌按钮。

## 5. 排版

### 5.1 字体

中国网络优先自托管：

- 中文/UI：`Noto Sans SC`；
- 拉丁 UI/标题：`Geist`（或经许可等价自托管字体）；
- 数字/代码/ID：`IBM Plex Mono` 或 `JetBrains Mono`。

禁止生产依赖 Google Fonts CDN。使用 `font-display: swap`、woff2 子集和预加载关键字重。

### 5.2 官网字号

| 角色 | 桌面 | 移动 | 行高 |
|---|---:|---:|---:|
| Display | 56–64 | 38–44 | 1.05–1.12 |
| H1 | 48–56 | 36–40 | 1.1 |
| H2 | 36–42 | 28–32 | 1.2 |
| H3 | 24–28 | 22–24 | 1.3 |
| Lead | 20 | 18 | 1.6 |
| Body | 16–18 | 16 | 1.7 |
| Small | 14 | 14 | 1.55 |

中文标题不要使用夸张字距；正文行宽 60–72 个拉丁字符等效，中文约 30–38 字。

### 5.3 RenWork 字号

桌面工具密度更高：Body 14，辅助 12–13，标题 16/20/24。关键数据用 tabular numerals。不得为“现代感”把业务表格正文降到 11px。

## 6. 网格、间距与响应式

- 8px 基础网格，允许 4px 微调；
- 官网最大内容宽 1200/1280px；文章正文 760px；
- 页面 section 桌面垂直间距 96–128px，移动 64–80px；
- 栅格 12 列，gap 24–32px；
- 断点：360、768、1024、1280、1440；
- 触控目标至少 44×44px；
- RenWork 表格可使用 32/40/48px 三种行高，用户可选密度。

不能依赖固定 1440 设计；每个页面需检查浏览器缩放 200% 和桌面窗口最小尺寸。

## 7. 形状、边框与阴影

- 官网卡片圆角 12–16px，按钮 8–10px；
- RenWork 控件遵循现有 DLS，不做全局圆角翻倍；
- 边框 `1px`，主要用于分组，不给每个容器套卡片；
- 阴影只用于浮层、产品截图和悬浮层级；
- 避免玻璃拟态导致低对比；
- 数据密集页面用间距和分隔线建立层级。

## 8. 图标与插图

- 只使用项目现有图标库；统一 1.5–2px stroke；
- 图标按钮必须有可访问名称和 tooltip；
- 不用 emoji 充当核心 UI 图标；
- 产品流程插图可使用简洁线框 + 局部品牌橙，不用复杂 3D；
- 图表颜色遵循状态语义，并提供 legend/tooltip/数据表。

## 9. 动效

| 类型 | 时长 | 用途 |
|---|---:|---|
| Micro | 140ms | hover、focus、toggle |
| Standard | 220ms | drawer、popover、tab |
| Emphasis | 320ms | 页面级重要进入 |

使用标准 ease-out，退出稍快。页面滚动动效只执行一次、位移 ≤ 24px，不阻塞阅读。支持 `prefers-reduced-motion`，数据表和任务进度不做装饰性无限动画。

## 10. 核心组件规范

### 10.1 Button

- Primary：`brand-orange-action` 背景 + 白字；hover 更深；
- Secondary：白/透明背景 + ink 边框；
- Tertiary：文字按钮；
- Destructive：red；
- 必须有 loading，loading 时保持宽度且防重复；
- 同屏最多一个主 CTA 层级。

### 10.2 Card

官网卡片：标题、价值、证据/状态、CTA；整卡可点时内部不要再嵌套冲突链接。RenWork 卡片优先信息密度，不把每一行包成卡片。

### 10.3 Table

固定表头、列显示、排序、筛选、保存视图、空状态、错误和虚拟化；批量操作只在选择后出现。数字右对齐，文本左对齐，状态列不只靠颜色。

### 10.4 Drawer/Detail

用于保留列表上下文；宽度根据内容 480–720px。复杂编辑或多标签详情使用独立路由，避免无限嵌套 drawer。

### 10.5 Form

标签常驻，不以 placeholder 代替；错误靠近字段并在顶部汇总；必填明确；提交结果可恢复。隐私与营销同意分开，不能预勾选。

### 10.6 Status Badge

统一映射：

| 语义 | 示例 | 颜色 |
|---|---|---|
| Neutral | Draft / Unverified | Gray |
| Info | Processing / Probable | Blue |
| Success | Verified / Published | Teal |
| Warning | Needs review / Degraded | Amber |
| Danger | Failed / Suppressed | Red |

## 11. 页面视觉模板

### 11.1 官网营销页

Hero（承诺 + 证据）→ 问题 → 工作流 → 产品界面 → 结果/案例 → 边界/安全 → FAQ → CTA。每个 section 只有一个中心问题。

### 11.2 官网文档页

左侧目录、中心正文、右侧页面 TOC；移动端收为抽屉。代码块、提示、警告、步骤和 API 表格有稳定样式。

### 11.3 RenWork 列表工作台

页面标题/上下文 → KPI/任务状态 → Filter bar → Table → Detail drawer → 批量动作。筛选状态入 URL/持久化。

### 11.4 RenWork 研究/撰写页

左：来源/证据；中：主编辑器；右：检查/审批/序列。窗口不足时右栏收为 tab/drawer，不能横向溢出。

## 12. 内容与微文案

- 按“用户动作 + 结果”命名，如“生成联系人候选”，不用“智能赋能”；
- 区分“已执行、建议、草稿、等待审批、失败”；
- 错误包含发生了什么、哪些数据安全、下一步；
- 危险确认明确对象和后果；
- 不用虚假紧迫感、伪实时计数或不可验证的百分比；
- 英文产品名保持一致：Buyer Intent、Contact Intelligence、LinkedIn 360、Outreach、Team Intelligence。

## 13. 可访问性

- WCAG 2.2 AA；普通文本 ≥ 4.5:1，大文本 ≥ 3:1；
- 逻辑 tab order、skip link、可见焦点；
- Dialog focus trap 与关闭后焦点恢复；
- 表格、图表和状态有语义文本；
- 图片有合适 alt；装饰图 `alt=""`；
- 表单错误与字段关联，并用 live region 宣告；
- 支持 200% 缩放、reduced motion、系统高对比；
- 不用 hover 作为唯一信息入口。

## 14. 美化实施顺序

### P0：一致性盘点

截图并列出 Header、Button、Card、Table、Form、Badge、Dialog、空/错/加载状态；识别重复组件和颜色漂移。先不改业务。

### P1：Token 与品牌资产

确认 Logo；补充语义 token；自托管字体；建立 light/dark 对照；不大规模改页面。

### P2：Shell 与核心组件

官网 Header/Footer，RenWork App Shell/导航，按钮/表单/状态/表格一致化。

### P3：关键页面

官网首页/产品/诊断/下载；RenWork Buyer、Contact、LinkedIn、Outreach。逐页做响应式和可访问验收。

### P4：动效与细节

统一过渡、空状态、图表、截图、微文案；删除纯装饰噪声。

## 15. 视觉 QA 清单

- Logo 未变形、未重绘，留白足够；
- 同一 CTA/状态跨页面一致；
- 360–1440px 和 200% zoom 无遮挡；
- 浅/深模式文本、边框、图表对比合格；
- loading 不导致布局跳动，empty/error 有下一步；
- 真实产品截图已脱敏，Demo 标识清晰；
- 无 Google Fonts 依赖、无未经批准第三方 Logo；
- 动效支持 reduced motion；
- 表格、表单、Dialog 键盘可完成；
- 页面没有为了“高级感”牺牲信息密度和可读性。

