extends: ./electron-builder.base.yml
appId: com.differentai.openwork
productName: RenWork Cloud
extraMetadata:
  openworkDistribution: cloud
protocols:
  - name: RenWork Cloud
    schemes:
      - openwork
publish:
  - provider: github
    owner: different-ai
    repo: openwork
    releaseType: release
    channel: cloud
linux:
  executableName: openwork-cloud
artifactName: openwork-cloud-${os}-${arch}-${version}.${ext}
