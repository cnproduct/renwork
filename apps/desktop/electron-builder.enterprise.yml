extends: ./electron-builder.base.yml
appId: com.differentai.openwork
productName: RenWork Enterprise
extraMetadata:
  openworkDistribution: enterprise
protocols:
  - name: RenWork Enterprise
    schemes:
      - openwork
publish:
  - provider: github
    owner: different-ai
    repo: openwork
    releaseType: release
    channel: enterprise
linux:
  executableName: renwork-enterprise
artifactName: renwork-enterprise-${os}-${arch}-${version}.${ext}
